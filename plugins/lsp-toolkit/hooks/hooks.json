{
  "description": "LSP-first methodology hooks - suggests using LSP over text-based search for semantic queries",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Grep|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "echo '<guideline>On supported files: .ts, .tsx, .js, .jsx, .go consider using LSP for semantic queries instead of text-based search.</guideline>'; exit 0"
          }
        ]
      }
    ]
  }
}
